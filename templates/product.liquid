{% assign current_variant = product.selected_or_first_available_variant %}
{% assign featured_image = current_variant.featured_image | default: product.featured_image %}
<img src="{{ featured_image | img_url: 'large' }}" alt="{{ featured_image.alt | escape }}" id="ProductPhotoImg">
{% for image in product.images %}
  <a href="{{ image.src | img_url: 'large' }}">
    <img src="{{ image.src | img_url: 'compact' }}" alt="{{ image.alt | escape }}">
  </a>
{% endfor %}
<h1>{{ product.title }}</h1>
<form action="/cart/add" method="post" enctype="multipart/form-data" id="AddToCartForm">
  <select name="id" id="productSelect">
    {% for variant in product.variants %}
      {% if variant.available %}
        <option value="{{ variant.id }}">
          {{ variant.title }} - {{ variant.price | money_with_currency }}
        </option>
      {% else %}
        <option disabled="disabled">
          {{ variant.title }} - sold out
        </option>
      {% endif %}
    {% endfor %}
  </select>
  {{ current_variant.price | money }}
    {% assign cart_index = cart.item_count | plus: 1 %}
    <input type="hidden" name="cart_index" value="{{ cart_index }}">
  <label for="Quantity">quantity</label>
    {%- assign result = product.variants.first.inventory_quantity | minus : cart.item_count -%}
    {% if result > 0 %}
  <input type="number" id="Quantity" name="quantity" value="1" min="1" max="{{ result }}">
  <button type="submit" name="add" id="add-to-cart-btn" class="add-cart-btn">Add to cart</button>
    {% else %}
  <input type="number" id="Quantity" name="quantity" value="0" value="0" disabled>
  <button type="submit" name="add" id="add-to-cart-btn" disabled>Add to cart</button>
    {% endif %}
</form>
<!-- Button trigger modal -->
<button type="button" class="modal-btn btn btn-primary" data-toggle="modal" data-target="#exampleModal">
    Launch demo modala
</button>
<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-body">
                <p>在庫：{{ product.variants.first.inventory_quantity }}</p>
                {% for item in cart.items %}
                <p>cart_index：{{ item }}</p>
                <p>商品名：{{ item.title }}</p>
                <p>数量：{{ item.quantity }}</p>
                {% endfor %}
                <p>カートの個数：{{ cart.item_count }}</p>
                <p>カートの合計金額：{{ cart.total_price | money}}</p>
            </div>
        </div>
    </div>
</div>
<div>{{ product.description }}</div>
